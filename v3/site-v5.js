(()=>{const D=window.GM_SNAPSHOT;if(!D||!window.GMV4)return;const P=id=>D.players[id]||{},ME=()=>D.teams.find(t=>t.id===D.my_team),fmt=x=>x==null||!isFinite(+x)?'—':`${x>=0?'+':''}${(+x).toFixed(1)}`,num=x=>x==null||!isFinite(+x)?'—':(+x).toFixed(1);function yr(p,y=2025){let e=(p?.elite_profile?.seasons||[]).find(z=>+z.season===+y);if(e)return{games:+e.games||0,ppg:+e.ppg||0,targets:+e.targets_pg||0,carries:+e.carries_pg||0,receptions:+e.receptions_pg||0,yards:(+e.rush_yd_pg||0)+(+e.rec_yd_pg||0),source:'nflverse'};let z=p?.history?.[String(y)];if(z){let g=Math.max(1,+z.games||1);return{games:+z.games||0,ppg:+z.ppg||(+z.pts_ppr||0)/g,targets:(+z.targets||0)/g,carries:(+z.carries||0)/g,receptions:(+z.receptions||0)/g,yards:((+z.rush_yd||0)+(+z.rec_yd||0))/g,source:'nflverse'}}if(+y===2025&&p?.historical_ppg_prior!=null)return{games:null,ppg:+p.historical_ppg_prior,targets:p.elite_profile?.targets_pg,carries:p.elite_profile?.carries_pg,receptions:p.elite_profile?.receptions_pg,yards:null,source:'weighted history fallback'};return null}function player(p){let ctx=p.current_context,g=p.nfl_schedule?.[0],h=yr(p);return`<div class="player-main"><span class="pos">${p.pos||'?'}</span><div><strong>${p.name}</strong><small>${p.team||'FA'}${ctx?.active?' · '+ctx.role:''}${p.injury?' · '+p.injury:''}${g?' · vs '+g.opponent:''}${h?' · 2025 '+num(h.ppg)+' PPR/G':''}</small></div></div>`}function cell(k,v,cls=''){return`<div class="dd-cell ${cls}"><small>${k}</small><b>${v}</b></div>`}function stats(p){let h=yr(p),w=p.weekly_context||{},ctx=p.current_context,ep=p.elite_profile||{};return`<div class="dd-grid">${cell('Position',p.pos||'—')}${cell('2025 PPR/G',h?num(h.ppg):num(p.historical_ppg_prior),'key')}${cell('2025 Games',h?.games??'—')}${cell('Targets/G',num(h?.targets??ep.targets_pg))}${cell('Carries/G',num(h?.carries??ep.carries_pg))}${cell('Receptions/G',num(h?.receptions??ep.receptions_pg))}${cell('Rush+Rec Yds/G',num(h?.yards))}${cell('Hist Floor',num(w.history_p25??ep.historical_floor))}${cell('Hist Median',num(w.history_median??ep.historical_ppg))}${cell('Hist Ceiling',num(w.history_p90??ep.historical_ceiling))}</div><div class="dd-source">Stats: ${h?.source||ep.source||'historical detail unavailable'}</div>${ctx?.active?`<div class="dd-role"><b>Current role:</b> ${ctx.role}<br><span>${ctx.reason||''}</span></div>`:''}`}function home(){let rows=GMV4.moveRows(),acts=rows.filter(x=>x.status==='ACT').slice(0,3),t=ME(),ctx=t.players.map(P).filter(p=>p.current_context?.active);return`<section class="hero"><div class="eyebrow">Jagger GM · audited</div><h1>${acts.length?`Your best move: ${acts[0].a.name} for ${acts[0].d.name}.`:'No forced waiver move right now.'}</h1><p>Every decision screen uses the same role-aware, position-aware roster model.</p></section>${ctx.length?`<section class="section"><div class="warning"><b>Breaking context:</b> ${ctx.map(p=>`${p.name} — ${p.current_context.role}`).join(' · ')}</div></section>`:''}<section class="section"><h2>Top actions</h2>${acts.length?acts.map((m,i)=>`<article class="card action"><div class="action-top"><div><div class="eyebrow">#${i+1}</div><h3>Add ${m.a.name} · Drop ${m.d.name}</h3></div><span class="badge claim">ACT</span></div><div class="minirow"><span class="chip good">Lineup ${fmt(m.lineup)} pts/wk</span><span class="chip">Role ${fmt(m.roleDelta)}</span><span class="chip">Confidence ${m.confidence}%</span></div></article>`).join(''):'<div class="card empty">Protect the roster. Manager HQ has the best WATCH options.</div>'}</section>`}function tradeRows(){let t=ME(),cur=GMV4.optimize(t.players),out=[];for(let opp of D.teams.filter(x=>x.id!==t.id)){let oc=GMV4.optimize(opp.players);for(let give of t.players.map(P).filter(p=>!['K','DEF'].includes(p.pos)))for(let get of opp.players.map(P).filter(p=>!['K','DEF'].includes(p.pos))){let mine=GMV4.optimize(t.players.filter(x=>x!==give.id).concat(get.id)),theirs=GMV4.optimize(opp.players.filter(x=>x!==get.id).concat(give.id)),mg=mine.total-cur.total,og=theirs.total-oc.total;if(mg>.25&&og>-.8){let gh=yr(give),rh=yr(get),ppg=gh&&rh?rh.ppg-gh.ppg:null,fit=Math.round(Math.max(0,Math.min(100,70+og*12-Math.abs(mg-og)*5)));out.push({opp,give,get,mg,og,ppg,fit})}}}return out.sort((a,b)=>b.mg-a.mg+(b.fit-a.fit)*.04).slice(0,40)}function reason(x){let a=yr(x.give),b=yr(x.get),s=`Your optimized lineup changes ${fmt(x.mg)} model pts/week; theirs changes ${fmt(x.og)}.`;if(a&&b)s+=` ${x.get.name}: ${num(b.ppg)} 2025 PPR/G vs ${x.give.name}: ${num(a.ppg)}.`;if(x.give.pos!==x.get.pos)s+=` This is a ${x.give.pos} → ${x.get.pos} roster-construction trade, so FLEX/position replacement matters.`;if(x.fit<55)s+=' Acceptance fit is weak.';return s}function trades(){let r=tradeRows();return`<section class="hero"><div class="eyebrow">Trade Lab · audited evidence</div><h1>Every trade shows the underlying stats.</h1><p>2025 actual production is read from the normalized nflverse season profile, with fallbacks explicitly labeled.</p></section><section class="section"><div class="grid two">${r.slice(0,16).map(x=>`<article class="card action"><div class="action-top"><div><div class="eyebrow">${x.opp.owner}</div><h3>${x.give.name} ⇄ ${x.get.name}</h3></div><span class="badge trade">FIT ${x.fit}</span></div><div class="tradebox"><div class="side"><small>YOU SEND</small>${player(x.give)}</div><div class="arrow">⇄</div><div class="side"><small>YOU GET</small>${player(x.get)}</div></div><div class="minirow"><span class="chip good">Your lineup ${fmt(x.mg)}</span><span class="chip ${x.og>=0?'good':'bad'}">Their lineup ${fmt(x.og)}</span><span class="chip">2025 PPR/G Δ ${fmt(x.ppg)}</span></div><div class="decision-details"><div class="dd-head"><b>Trade evidence</b><span>2025 actuals + current role</span></div><div class="dd-compare"><div class="dd-player"><div class="dd-title"><span>YOU SEND</span><strong>${x.give.name}</strong></div>${stats(x.give)}</div><div class="dd-player"><div class="dd-title"><span>YOU GET</span><strong>${x.get.name}</strong></div>${stats(x.get)}</div></div></div><p class="reason"><b>Why:</b> ${reason(x)}</p></article>`).join('')||'<div class="card empty">No clean 1-for-1 deal currently improves your lineup without badly hurting the other roster.</div>'}</div></section>`}function league(){let rows=D.teams.map(t=>({t,o:GMV4.optimize(t.players)})).sort((a,b)=>b.o.total-a.o.total);return`<section class="hero"><div class="eyebrow">League X-Ray</div><h1>Every team uses the same lineup model.</h1></section><section class="section"><div class="table"><table><thead><tr><th>#</th><th>Manager</th><th>Optimized</th><th>Role alerts</th></tr></thead><tbody>${rows.map((x,i)=>`<tr><td>${i+1}</td><td><b>${x.t.owner}</b>${x.t.id===D.my_team?' · YOU':''}</td><td>${x.o.total.toFixed(1)}</td><td>${x.t.players.map(P).filter(p=>p.current_context?.active).map(p=>p.name+': '+p.current_context.role).join(' · ')||'—'}</td></tr>`).join('')}</tbody></table></div></section>`}function news(){let role=Object.values(D.players).filter(p=>p.current_context?.active),items=D.news||[];return`<section class="hero"><div class="eyebrow">News & Role Center</div><h1>What changed — and whether the model used it.</h1></section><section class="section">${role.map(p=>`<article class="card">${player(p)}<p class="reason">${p.current_context.reason}</p></article>`).join('')||'<div class="card empty">No active role overrides.</div>'}</section><section class="section">${items.slice(0,25).map(n=>`<article class="card"><a href="${n.url}" target="_blank" rel="noopener" style="color:inherit;text-decoration:none"><b>${n.title}</b></a></article>`).join('')}</section>`}function install(){let dock=document.querySelector('#dock');if(!dock)return;for(let [tab,fn] of Object.entries({home,trades,league,news})){let b=dock.querySelector(`[data-tab="${tab}"]`);if(b)b.onclick=()=>{dock.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));document.querySelector('#view').innerHTML=fn();scrollTo({top:0,behavior:'smooth'})}}let p=dock.querySelector('[data-tab="power"]');if(p)p.onclick=()=>{dock.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===p));document.querySelector('#view').innerHTML=GMV4.hq();scrollTo({top:0,behavior:'smooth'})}}install()})();