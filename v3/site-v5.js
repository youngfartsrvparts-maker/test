(()=>{const D=window.GM_SNAPSHOT;if(!D||!window.GMV4)return;const P=id=>D.players[id]||{},ME=()=>D.teams.find(t=>t.id===D.my_team),fmt=x=>x==null?'—':`${x>=0?'+':''}${(+x).toFixed(1)}`;function player(p){let ctx=p.current_context,g=p.nfl_schedule?.[0];return`<div class="player-main"><span class="pos">${p.pos||'?'}</span><div><strong>${p.name}</strong><small>${p.team||'FA'}${ctx?.active?' · '+ctx.role:''}${p.injury?' · '+p.injury:''}${g?' · vs '+g.opponent:''}</small></div></div>`}function home(){let rows=GMV4.moveRows(),acts=rows.filter(x=>x.status==='ACT').slice(0,3),t=ME(),ctx=t.players.map(P).filter(p=>p.current_context?.active);return`<section class="hero"><div class="eyebrow">Jagger GM v5 · one decision system</div><h1>${acts.length?`Your best move: ${acts[0].a.name} for ${acts[0].d.name}.`:'No forced waiver move right now.'}</h1><p>Every decision screen now uses the same role-aware, position-aware roster model. Current verified role changes can override stale history.</p></section>${ctx.length?`<section class="section"><div class="warning"><b>Breaking context affecting your roster:</b> ${ctx.map(p=>`${p.name} — ${p.current_context.role}`).join(' · ')}</div></section>`:''}<section class="section"><div class="section-head"><div><h2>Top actions</h2></div></div>${acts.length?acts.map((m,i)=>`<article class="card action"><div class="action-top"><div><div class="eyebrow">#${i+1}</div><h3>Add ${m.a.name} · Drop ${m.d.name}</h3></div><span class="badge claim">ACT</span></div><div class="minirow"><span class="chip good">Lineup ${fmt(m.lineup)} pts/wk</span><span class="chip">Role ${fmt(m.roleDelta)}</span><span class="chip">Confidence ${m.confidence}%</span></div></article>`).join(''):'<div class="card empty">Protect the roster. Manager HQ has the best WATCH options.</div>'}</section>`}function tradeRows(){let t=ME(),cur=GMV4.optimize(t.players),out=[];for(let opp of D.teams.filter(x=>x.id!==t.id)){let oc=GMV4.optimize(opp.players);for(let give of t.players.map(P).filter(p=>!['K','DEF'].includes(p.pos)))for(let get of opp.players.map(P).filter(p=>!['K','DEF'].includes(p.pos))){let mine=GMV4.optimize(t.players.filter(x=>x!==give.id).concat(get.id)),theirs=GMV4.optimize(opp.players.filter(x=>x!==get.id).concat(give.id)),mg=mine.total-cur.total,og=theirs.total-oc.total;if(mg>.25&&og>-.8)out.push({opp,give,get,mg,og,fit:Math.round(Math.max(0,Math.min(100,70+og*12-Math.abs(mg-og)*5)))})}}return out.sort((a,b)=>b.mg-a.mg+(b.fit-a.fit)*.04).slice(0,40)}function trades(){let r=tradeRows();return`<section class="hero"><div class="eyebrow">Trade Lab · v5</div><h1>Trades are simulated on both rosters.</h1><p>A deal only rises if your optimized lineup improves and the other manager is not being asked to destroy theirs.</p></section><section class="section"><div class="grid two">${r.slice(0,16).map(x=>`<article class="card"><div class="action-top"><div><div class="eyebrow">${x.opp.owner}</div><h3>${x.give.name} ⇄ ${x.get.name}</h3></div><span class="badge trade">FIT ${x.fit}</span></div><div class="tradebox"><div class="side"><small>YOU SEND</small>${player(x.give)}</div><div class="arrow">⇄</div><div class="side"><small>YOU GET</small>${player(x.get)}</div></div><div class="minirow"><span class="chip good">Your lineup ${fmt(x.mg)}</span><span class="chip ${x.og>=0?'good':'bad'}">Their lineup ${fmt(x.og)}</span></div></article>`).join('')||'<div class="card empty">No clean 1-for-1 deal currently improves your lineup without badly hurting the other roster.</div>'}</div></section>`}function league(){let rows=D.teams.map(t=>{let o=GMV4.optimize(t.players);return{t,o}}).sort((a,b)=>b.o.total-a.o.total);return`<section class="hero"><div class="eyebrow">League X-Ray · v5</div><h1>Every team graded with the same lineup model.</h1><p>No separate legacy power score.</p></section><section class="section"><div class="table"><table><thead><tr><th>#</th><th>Manager</th><th>Optimized</th><th>Current role alerts</th></tr></thead><tbody>${rows.map((x,i)=>`<tr><td>${i+1}</td><td><b>${x.t.owner}</b>${x.t.id===D.my_team?' · YOU':''}</td><td>${x.o.total.toFixed(1)}</td><td>${x.t.players.map(P).filter(p=>p.current_context?.active).map(p=>p.name+': '+p.current_context.role).join(' · ')||'—'}</td></tr>`).join('')}</tbody></table></div></section>`}function news(){let role=Object.values(D.players).filter(p=>p.current_context?.active),items=D.news||[];return`<section class="hero"><div class="eyebrow">News & Role Center</div><h1>What changed — and whether the model used it.</h1><p>Verified role changes materially affect recommendations. Headline-only signals are deliberately smaller until corroborated.</p></section><section class="section"><div class="section-head"><div><h2>Active role changes</h2></div></div>${role.map(p=>`<article class="card"><div class="action-top">${player(p)}<span class="badge ${p.current_context.type==='verified'?'claim':'watch'}">${p.current_context.confidence}%</span></div><p class="reason">${p.current_context.reason}</p><div class="minirow">${(p.current_context.sources||[]).map(s=>`<a class="chip" href="${s.url}" target="_blank" rel="noopener">${s.name}</a>`).join('')}</div></article>`).join('')||'<div class="card empty">No active role overrides.</div>'}</section><section class="section"><div class="section-head"><div><h2>Recent headlines</h2></div></div>${items.slice(0,25).map(n=>`<article class="card"><a href="${n.url}" target="_blank" rel="noopener" style="color:inherit;text-decoration:none"><b>${n.title}</b></a><p class="reason">${n.source||'News'} · ${n.published?new Date(n.published).toLocaleString():''}</p></article>`).join('')}</section>`}function install(){let dock=document.querySelector('#dock');if(!dock)return;let map={home, trades, league, news};for(let [tab,fn] of Object.entries(map)){let b=dock.querySelector(`[data-tab="${tab}"]`);if(!b)continue;b.onclick=()=>{dock.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));document.querySelector('#view').innerHTML=fn();scrollTo({top:0,behavior:'smooth'})}}let p=dock.querySelector('[data-tab="power"]');if(p)p.onclick=()=>{dock.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===p));document.querySelector('#view').innerHTML=GMV4.hq();scrollTo({top:0,behavior:'smooth'})}}install()})();