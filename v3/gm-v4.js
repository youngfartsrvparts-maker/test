(()=>{'use strict';const D=window.GM_SNAPSHOT;if(!D)return;const P=id=>D.players[id]||{},ME=()=>D.teams.find(t=>t.id===D.my_team),owned=()=>new Set(D.teams.flatMap(t=>t.players||[]));const clamp=(x,a=0,b=100)=>Math.max(a,Math.min(b,x)),fmt=x=>x==null?'—':`${x>=0?'+':''}${(+x).toFixed(1)}`;function hp(p){let h=p.elite_profile||{};return{ppg:h.historical_ppg??p.historical_ppg_prior??null,opp:h.opportunities_pg??null,evidence:h.evidence??Math.round((p.history_confidence||0)*100),floor:p.weekly_context?.history_p25??h.historical_floor??null,median:p.weekly_context?.history_median??h.historical_ppg??null,ceiling:p.weekly_context?.history_p90??h.historical_ceiling??null,zero:p.weekly_context?.history_zero_rate??null}}function health(p){if(!p.injury)return 1;let s=String(p.injury).toLowerCase();return s.includes('out')||s.includes('ir')?0:s.includes('doubt')?.35:s.includes('question')?.72:.6}function currentWeight(){let n=Math.max(...Object.values(D.players).map(p=>p.weekly_context?.current_games?.length||0));return Math.min(.82,n/6*.82)}function baseline(p){let h=hp(p),c=p.weekly_context||{},cw=currentWeight(),hist=h.ppg??(p.draft_pick?Math.max(4,17-p.draft_pick*.08):6),cur=c.current_ppg;let raw=cur!=null?cur*cw+hist*(1-cw):hist;let role=0;if(c.current_opportunities_pg!=null)role+=(c.current_opportunities_pg-10)*.12;else if(h.opp!=null)role+=(h.opp-10)*.08;if(p.depth)role+=Math.max(-1.5,1.3-(+p.depth*.45));let youth=(+p.experience||9)<=2?.45:0,market=Math.max(-.35,Math.min(.35,((+p.adds||0)-(+p.drops||0))/30000)),game=p.nfl_schedule?.[0],environment=game?.implied_team_points!=null?(game.implied_team_points-22)*.045:0;return Math.max(0,(raw+role+youth+market+environment)*health(p))}function uncertainty(p){let h=hp(p),e=h.evidence||0,u=1+(100-e)/100*3;if((+p.experience||9)<=1)u+=1.5;if(p.injury)u+=1.5;return u}function slots(){return(D.league.slots||[]).filter(x=>!['BN','IR','RESERVE','TAXI'].includes(x))}function eligible(p,s){return p.pos===s||(s==='FLEX'&&['RB','WR','TE'].includes(p.pos))||(s==='SUPER_FLEX'&&['QB','RB','WR','TE'].includes(p.pos))||(s==='REC_FLEX'&&['WR','TE'].includes(p.pos))}function optimize(ids){let pool=ids.map(P),used=new Set(),line=[];for(let s of slots()){let c=pool.filter(p=>!used.has(p.id)&&eligible(p,s)).sort((a,b)=>baseline(b)-baseline(a))[0];if(c){used.add(c.id);line.push({slot:s,p:c,pts:baseline(c)})}}return{line,total:line.reduce((a,x)=>a+x.pts,0),used}}function free(){let o=owned();return Object.values(D.players).filter(p=>!o.has(p.id)&&p.team&&p.active!==false&&['QB','RB','WR','TE','K','DEF'].includes(p.pos))}function droppable(){let t=ME(),st=new Set(t.starters||[]);return t.players.map(P).filter(p=>!t.reserve.includes(p.id)&&!['K','DEF'].includes(p.pos)&&!st.has(p.id))}function strategicValue(p){let h=hp(p),b=baseline(p),up=(h.ceiling??b)-b,scar={RB:1.12,WR:1.04,TE:1.02,QB:.82,K:.45,DEF:.5}[p.pos]||1;return b*scar+Math.max(0,up)*.28+((+p.experience||9)<=2?1.1:0)-injuryPenalty(p)}function injuryPenalty(p){return(1-health(p))*4}function moveRows(){let t=ME(),cur=optimize(t.players),rows=[];for(let a of free())for(let d of droppable()){let ids=t.players.filter(x=>x!==d.id).concat(a.id),after=optimize(ids),lineup=after.total-cur.total,bench=strategicValue(a)-strategicValue(d),ah=hp(a),dh=hp(d),hist=ah.ppg!=null&&dh.ppg!=null?ah.ppg-dh.ppg:null,confidence=Math.round(clamp(((ah.evidence||0)+(dh.evidence||0))/2-(a.injury?12:0)-(ah.ppg==null||dh.ppg==null?15:0),20,95)),upside=(ah.ceiling!=null&&dh.ceiling!=null?ah.ceiling-dh.ceiling:0),score=lineup*12+bench*2.3+(hist??0)*2.4+upside*.7;if(hist!=null&&hist<-3&&lineup<.5)score-=18;let status=lineup>=1&&confidence>=55?'ACT':(score>4&&confidence>=40?'WATCH':'HOLD');if(hist!=null&&hist<-2&&lineup<=0)status='HOLD';rows.push({a,d,lineup,bench,hist,upside,confidence,score,status,after})}return rows.sort((x,y)=>y.score-x.score)}function bestByFA(){let seen=new Set(),out=[];for(let m of moveRows()){if(seen.has(m.a.id))continue;seen.add(m.a.id);out.push(m)}return out}function player(p){let h=hp(p),g=p.nfl_schedule?.[0];return`<div class="player-main"><span class="pos">${p.pos||'?'}</span><div><strong>${p.name}</strong><small>${p.team||'FA'}${p.injury?' · '+p.injury:''}${h.ppg!=null?' · HIST '+h.ppg.toFixed(1)+' PPR/G':''}${g?' · vs '+g.opponent:''}</small></div></div>`}function badge(s){return`<span class="badge ${s==='ACT'?'claim':s==='WATCH'?'watch':'pass'}">${s}</span>`}function card(m,i){let ah=hp(m.a),dh=hp(m.d),title=m.status==='ACT'?`Add ${m.a.name} · Drop ${m.d.name}`:m.status==='WATCH'?`Watch ${m.a.name} · Keep ${m.d.name} for now`:`Keep ${m.d.name} over ${m.a.name}`;return`<article class="card action"><div class="action-top"><div><div class="eyebrow">Decision #${i+1}</div><h3>${title}</h3></div>${badge(m.status)}</div><div class="tradebox"><div class="side"><small>CURRENT</small>${player(m.d)}</div><div class="arrow">→</div><div class="side"><small>OPTION</small>${player(m.a)}</div></div><div class="minirow"><span class="chip ${m.lineup>0?'good':m.lineup<0?'bad':''}">Starting lineup ${fmt(m.lineup)} pts/wk</span><span class="chip ${m.hist>0?'good':m.hist<0?'bad':''}">Hist PPR/G ${fmt(m.hist)}</span><span class="chip">Bench value ${fmt(m.bench)}</span><span class="chip">Upside ${fmt(m.upside)}</span><span class="chip">Confidence ${m.confidence}%</span></div><p class="reason">${m.lineup>0?`This move improves your currently optimized starting lineup by about ${m.lineup.toFixed(1)} model points per week. `:'This does not currently improve your starting lineup. '}${m.hist!=null?`${m.a.name} has a ${fmt(m.hist)} historical PPR/G difference versus ${m.d.name}. `:''}${m.status==='ACT'?'Evidence clears the conservative action threshold.':m.status==='WATCH'?'Preserve your current player until role/news/Week 1 evidence strengthens.':'Do not make this swap based on the evidence currently loaded.'}</p></article>`}function hq(){let rows=moveRows(),act=rows.filter(x=>x.status==='ACT').slice(0,5),watch=rows.filter(x=>x.status==='WATCH').slice(0,5),cur=optimize(ME().players);let top=[...act,...watch].slice(0,8);return`<section class="hero"><div class="eyebrow">Manager HQ · unified v4 engine</div><h1>${act.length?`${act.length} actionable upgrade${act.length>1?'s':''} found.`:'No move is strong enough to force right now.'}</h1><p>Advice is now based first on whether a transaction improves your actual starting lineup. Historical production, current usage, injury status, role, schedule environment, upside and bench value are supporting evidence—not substitutes for lineup impact.</p></section><div class="metrics"><div class="metric"><span>Optimized baseline</span><b>${cur.total.toFixed(1)}</b><small>model pts/week</small></div><div class="metric"><span>Actionable</span><b>${act.length}</b><small>threshold cleared</small></div><div class="metric"><span>2026 games loaded</span><b>${Math.max(...Object.values(D.players).map(p=>p.weekly_context?.current_games?.length||0))}</b><small>weight rises automatically</small></div><div class="metric"><span>Scenarios</span><b>${free().length*droppable().length}</b><small>waiver combinations</small></div></div><section class="section"><div class="section-head"><div><h2>What to do now</h2><p>Only transactions that survive the full roster simulation rise to the top.</p></div></div>${top.length?top.map(card).join(''):'<div class="card empty">Hold your roster. No available move currently clears the evidence threshold.</div>'}</section>`}function waivers(){let rows=bestByFA();return`<section class="hero"><div class="eyebrow">Waivers · unified v4 engine</div><h1>Ranked by roster improvement, not hype.</h1><p>Each free agent is paired with the best realistic drop and the entire lineup is re-optimized after the move.</p></section><section class="section"><div class="controls"><input id="v4q" class="control search" placeholder="Search player"><select id="v4p" class="control"><option>ALL</option><option>RB</option><option>WR</option><option>TE</option><option>QB</option><option>DEF</option><option>K</option></select></div><div id="v4list" class="list" style="margin-top:10px"></div></section>`}function drawWaivers(){let q=(document.querySelector('#v4q')?.value||'').toLowerCase(),pos=document.querySelector('#v4p')?.value||'ALL',rows=bestByFA().filter(m=>(pos==='ALL'||m.a.pos===pos)&&m.a.name.toLowerCase().includes(q)).slice(0,60);document.querySelector('#v4list').innerHTML=rows.map(card).join('')||'<div class="card empty">No matching players.</div>'}function roster(){let t=ME(),o=optimize(t.players),start=new Set(o.line.map(x=>x.p.id));return`<section class="hero"><div class="eyebrow">Roster Optimizer</div><h1>Your strongest roster right now.</h1><p>The lineup below is optimized using the same evidence model that evaluates every waiver move.</p></section><section class="section"><div class="grid two">${o.line.map(x=>`<article class="card"><div class="eyebrow">${x.slot}</div><div class="player-row">${player(x.p)}<span class="score">${x.pts.toFixed(1)}</span></div></article>`).join('')}</div></section><section class="section"><div class="section-head"><div><h2>Bench</h2></div></div><div class="grid two">${t.players.map(P).filter(p=>!start.has(p.id)).sort((a,b)=>strategicValue(b)-strategicValue(a)).map(p=>`<article class="card">${player(p)}<div class="minirow"><span class="chip">Baseline ${baseline(p).toFixed(1)}</span><span class="chip">Strategic ${strategicValue(p).toFixed(1)}</span></div></article>`).join('')}</div></section>`}function install(){let dock=document.querySelector('#dock');if(!dock)return;let h=dock.querySelector('[data-tab="hq"]'),w=dock.querySelector('[data-tab="waivers"]'),t=dock.querySelector('[data-tab="team"]');function activate(b,fn,after){b.onclick=()=>{dock.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));document.querySelector('#view').innerHTML=fn();if(after)after();scrollTo({top:0,behavior:'smooth'})}}if(h)activate(h,hq);if(w)activate(w,waivers,()=>{drawWaivers();document.querySelector('#v4q').oninput=drawWaivers;document.querySelector('#v4p').onchange=drawWaivers});if(t)activate(t,roster)}install();window.GMV4={baseline,optimize,moveRows,bestByFA,hq,roster}})();